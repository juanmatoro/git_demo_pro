---
const { pathname } = Astro.url;

const links = [
  { href: '/', label: 'Inicio' },
  { href: '/recetas', label: 'Recetas' },
  { href: '/menus', label: 'Menus' },
  { href: '/practicas', label: 'Practicas Git' },
  { href: '/astro', label: 'Aprende Astro' },
];

function isActive(href: string) {
  if (href === '/') return pathname === '/';
  return pathname.startsWith(href);
}
---

<nav class="nav">
  <a href="/" class="logo">Recetario de GIT</a>
  <ul>
    {links.map(({ href, label }) => (
      <li>
        <a href={href} class={isActive(href) ? 'active' : ''}>{label}</a>
      </li>
    ))}
  </ul>
</nav>

<style>
  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 0;
    border-bottom: 2px solid #c97b3c;
    margin-bottom: 24px;
  }

  .logo {
    font-size: 1.4rem;
    font-weight: bold;
    color: #c97b3c;
    text-decoration: none;
  }

  ul {
    list-style: none;
    display: flex;
    gap: 18px;
    padding: 0;
    margin: 0;
  }

  a {
    color: #5a3a1f;
    text-decoration: none;
    font-size: 0.95rem;
  }

  a:hover {
    color: #c97b3c;
  }

  a.active {
    color: #c97b3c;
    font-weight: bold;
    border-bottom: 2px solid #c97b3c;
    padding-bottom: 2px;
  }
</style>
